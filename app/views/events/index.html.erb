<ul>
  <% @events.each do |e| %>
    <li>
      <strong><%= e.name %></strong>
      in <%= e.location %>:
      <%= number_to_currency(e.price, precision: 0) %>
    </li>
    <p>
      <%= e.description %>
    </p>
    <p>
      <%= e.starts_at %>
    </p>
  <% end %>
</ul>